/*

temporal dead zone:- it is the time between the start a block scope and when the veriable declared with let and const at initialized.accesing the veriable during the tdz it throws a referenceerror.
*/

console.log(x);
let x=10;